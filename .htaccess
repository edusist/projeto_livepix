RewriteEngine On
RewriteBase /

# Se o arquivo ou diretório existir, entrega normalmente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Caso contrário, tudo vai para o index.php
RewriteRule ^ index.php [QSA,L]
